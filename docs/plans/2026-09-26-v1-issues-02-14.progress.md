# 开发进度：鸣潮工具箱 V1 issue 02–14 批量开发

- 方案文件：docs/plans/2026-09-26-v1-issues-02-14.md
- 运行方式：无人值守
- 开始时间：2026-09-26 12:53
- 分支 / 起始 HEAD：main / a9adbc7
- 开工时工作区已有的未提交改动（属于用户，不碰）：
  - 无（`git status --porcelain` 为空；方案与进度文件在开工准备中新建，属本次运行产物）
- 基线：构建 通过（`npm run build` 含 vue-tsc）；基线测试范围：回归集 = `npm test`（全部前端测试，6/6 通过）+ `cargo test`（Rust 单测，当前 0 个，通过）+ `cargo check` 通过；开工前已失败的测试：无
- 温控档位（macOS 适配，方案 D6）：串行；`vitest` 单 fork；`cargo -j 4`；任务间 `sleep 60` 冷却

## 任务状态

| 任务 | 标题 | 状态 | 返工轮数 | commit | 备注 |
| ---- | ---- | ---- | -------- | ------ | ---- |
| T02 | 粘贴链接导入端到端 | 已完成 | 1 | 2f7b39b | code-reviewer 复核：1 MAJOR + 3 MINOR 已全部修复 |
| T03 | 全池同步编排与进度 | 已完成 | 0 | 59b4da1 | 偏差：非失效类错误（如重试耗尽）也中止全池，保守处理 |
| T04 | 目录探测与一键同步 | 已完成 | 1 | d34a579 | code-reviewer：1 MAJOR+5 MINOR，#1-#4/#6 采纳修复；#5 部分采纳（手动目录优先，候选改选 UI 留 #13）；#6 网络盘启发式留 #14 |
| T05 | 多 UID 选择与档案切换 | 已完成 | 0 | 9665987 | 偏差：UID 选择列表选定即确认（不二次弹切换确认），依 CONTEXT 术语 |
| T06 | 头像资产与常驻名单数据 | 已完成 | 0 | 3d4fda4 | 偏差：resourceId 映射表留空（社区无可靠数据），判定走双语名匹配；其他语言记录标「未知」不计歪率 |
| T07 | 统计评语核心 + 本池评语行 | 已完成 | 0 | 0b65537 | 假设：池 6/7 保底按 80；样本不足评级「—」；V1 评语行默认池 code 1（#09 接管联动） |
| T08 | 五星编年史名册 | 已完成 | 0 | 30ee7f7 | 偏差：属性小标展示 resourceType（记录无属性字段，数据可达上限） |
| T09 | 池页签与联动汇总 | 已完成 | 0 | 28c3df2 | 偏差：页签状态放 App 本地（单画面 UI 状态，无需持久化） |
| T10 | 进行中卡、保底引线与高光纪念牌 | 已完成 | 0 | de01d79 | 口径：旅途总览=全部卡池；最欧/最非=角色系限定池；引线固定池 1/2/3 |
| T11 | 记录抽屉 | 已完成 | 0 | de5f7be | 偏差：筛选行固定不随滚动（窗口化渲染需要）；T02 RecordList 过渡件已删 |
| T12 | 设置弹窗与备份恢复/清空 | 已完成 | 0 | c520f5f | 文件 IO 选 Rust 薄命令（不引 plugin-fs）；清空后保持当前 UID 空态（保守） |
| T13 | 偏好设置：主题/自动同步/目录管理 | 已完成 | 0 | cdae7b9 | 偏差：缓存链接失效后保留（每次启动仅 1 次失败请求）；UID 异档时静默跳过 |
| T14 | 便携版打包与 V1 真机验收 | 已跳过 | — | — | macOS 不可执行 Windows 构建/真机验收；教程 docs/BUILD-WINDOWS.md 已交付（随本 commit 提交），issue #14 留 Comments |

## 运行日志

- 12:53 开工准备：方案与进度文件落盘，待跑基线
- 12:55 基线全绿：npm test 6/6、npm run build（vue-tsc）通过、cargo check 通过、cargo test 0 个。T02 派单
- 13:20 T02 developer 交付（43 前端 + 9 Rust 测试全过）→ 主编验证通过 → 请 code-reviewer：1 MAJOR（重试语义不对称）+ 3 MINOR，全部采纳
- 13:44 T02 第 1 轮返工交付：envelope 校验移入重试层 + Rust HTTP status 分类（kind network/fatal）、URL 结尾杂讯剔除、busy_timeout 3s、按钮 span。主编复验 43/43 + 12/12 + build + lint 全过 → commit 2f7b39b，冷却 60s
- 14:00 T03 派单 → 交付（52 前端测试全过）→ 主编通读 diff 无需 reviewer → commit 59b4da1，冷却 60s
- 14:12 T04 派单（最大任务）→ 15:00 交付（cargo 45 + 前端 65 全过；XOR 按字节值奇偶实现，与 research §1.2 逐字一致）→ code-reviewer：1 MAJOR（Engine.ini 关日志诊断死循环）+ 5 MINOR
- 15:29 T05 交付（97 前端测试）→ commit 9665987，冷却
- 15:59 T06 交付（120 测试；resourceId 映射表因社区无可靠数据留空，判定走双语名匹配）→ commit 3d4fda4，冷却
- 16:22 T07 交付（175 测试，档位边界逐一验证）→ commit 0b65537，冷却
- 16:44 T08 交付（199 测试）→ commit 30ee7f7，冷却
- 17:02 T09 交付（221 测试）→ commit 28c3df2，冷却
- 17:24 T10 交付（255 测试）→ commit de01d79，冷却
- 17:43 T11 交付（280 测试；T02 RecordList 过渡件按计划移除）→ commit de5f7be，冷却
- 18:05 T12 交付（317 前端 + 52 Rust 测试）→ commit c520f5f，冷却
- 18:27 T13 交付（352 前端测试）→ commit cdae7b9，冷却
- 18:30 T14：Windows 构建与真机验收在 macOS 不可执行，按用户指示跳过；教程 docs/BUILD-WINDOWS.md 交付；issue #14 追加 Comments，issue 02–13 置 ready-for-human（沿用 #01 惯例）
- 18:32 收尾总验收全绿：npm test 352/352、npm run build（vue-tsc + vite）通过、lint 0 问题、cargo check 通过、cargo test 52/52。对比基线（6/6 前端 + 0 Rust）无新增失败

## 跳过与假设

### T14 已跳过（Windows 专属部分）

- 卡在哪里：便携版 zip 构建需 Windows + MSVC 工具链（ADR-0001、ADR-0008），真机验收（真实账号、抓包、XOR 日志、性能）需 Windows 真机；macOS 开发机均不可执行。
- 试过什么：`rustup target add x86_64-pc-windows-msvc` 交叉 `cargo check`——cfg(windows) 业务代码可过（T04 曾以临时壳 crate 验证 winprobe 编译），但依赖 `ring` 的 C 构建脚本失败，无法出完整产物。
- 主编判断：按用户原始指示"做不了先不做，留下教程手动操作"处理；交付 `docs/BUILD-WINDOWS.md`（含 15 项真机验收清单），Windows 侧完成后回灌新票。
- 现场保留：无需 stash（T14 无代码改动，教程已完整交付并提交）。

### code-reviewer 意见的处置（T02、T04 各一次）

- T02：1 MAJOR（重试语义不对称）+ 3 MINOR → 全部采纳，第 1 轮返工修复（commit 2f7b39b）。
- T04：1 MAJOR（Engine.ini 关日志诊断死循环）+ 5 MINOR → #1/#2/#3/#4 采纳修复；#5 部分采纳（记住的手动目录排最前，候选改选 UI 留 #13 收口）；#6 半采纳（盘符枚举复用；网络映射盘启发式留 #14 真机验证）。第 1 轮返工修复（commit d34a579）。

### developer 报告的任务外风险（仅记录，留给用户）

- T02：reqwest 用 rustls（bundled webpki 根证书），Windows 真机首验时关注官方接口证书链。
- T04：io-error 诊断码暂无前端专门指引文案（#13/#14 可收口）；WeGame 安装前缀为运行时推测。
- T08：主画面 overflow:hidden 后超长消息行可能挤压名册高度（当前文案量级无碍）。
- T11：.rec-row 行高 36px 与 RECORD_ROW_HEIGHT 常量两处表达，改行高需同步。
- T12：抽屉与设置弹窗同时打开时 ESC 会同时关闭两者（实践中难同时触发）。
- T13：自动同步成功完全无提示（产品口径，如需轻提示再议）。

### 过程中做的假设

- 测试范围约定（D5）：本仓库回归集 = `npm test` + `cargo test` 全量（均为快速单测/组件测试），无其他全量套件；从未引入 e2e/真实网络/真机测试。
- issue 文案「一键同步」按 CONTEXT.md 术语统一落地为「一键获取」；产品名按最新定名「鸣潮工具箱」（issue 中「唤取档案」为旧工作名）。
- T04：issue「手动指定列末位」改为「记住的手动目录有效时排最前」（用户显式选择优先，防 MuiCache 陈旧条目死路）。
- T05：多 UID 选择列表选定即确认，不再二次弹切换确认（依 CONTEXT「切换档案」）。
- T06：resourceId 映射表留空（社区无可靠数据，宁缺勿滥），歪判定走中/英文名匹配；非中英文语言的记录标「未知」不计歪率。
- T07：新手自选（code 6）/感恩定向（code 7）硬保底按 80；样本不足评级「—」；V1 评语行默认池 code 1（T09 收口为页签联动）。
- T09：页签选中状态放 App 本地（单画面 UI 状态）。
- T10：旅途总览口径 = 全部卡池；最欧/最非口径 = 角色系限定池；保底引线固定池 1/2/3。
- T12：备份文件 IO 用 Rust 薄命令（不引 plugin-fs）；清空后保持当前 UID 空态。
- T13：自动同步缓存链接失效后保留缓存（每次启动仅 1 次失败请求）；UID 与当前档案不同时静默跳过。
- 偏好持久化统一用 localStorage（theme/autoSync/lastSyncUrl/gameDir 四个 key，集中 stores/preferences.ts）。

## 总结报告

- 结束时间 / 总耗时：2026-09-26 18:35 / 约 5 小时 40 分（12:53 开工）
- 结果：完成 12 个（T02–T13），跳过 1 个（T14，Windows 专属部分，教程已交付），被阻塞 0 个
- 收尾构建：`npm run build`（vue-tsc --noEmit + vite）通过；`cargo check` 通过；lint 0 问题
- 收尾测试：回归集全量——前端 352/352 通过（37 文件）、Rust 52/52 通过；对比基线（前端 6 个、Rust 0 个）零失败。未跑全量之外任何无关测试（本仓库无其他测试套件）
- 每任务一个 commit：2f7b39b(#02) → 59b4da1(#03) → d34a579(#04) → 9665987(#05) → 3d4fda4(#06) → 0b65537(#07) → 30ee7f7(#08) → 28c3df2(#09) → de01d79(#10) → de5f7be(#11) → c520f5f(#12) → cdae7b9(#13)，均在 main 分支、未 push
- developer 报告的任务外风险：见上节
- code-reviewer 留下的未处理 MINOR：无（两次审查发现全部处置完毕）
- 需要用户处理的事项：
  1. 在 Windows 机按 `docs/BUILD-WINDOWS.md` 构建便携 zip 并执行 15 项真机验收（T14 剩余部分），问题回灌 issues 新票
  2. resourceId ↔ 物品映射表目前为空（社区无可靠数据）：如后续获得可靠数据，补录 `src/domain/avatars.ts` 与 `src/domain/standardRoster.ts` 的 resourceId 字段即可生效
  3. 常驻名单（6 角色 + 5 武器）与实际游戏版本核对一次；官方并入新角色时更新 `src/domain/standardRoster.ts`
  4. 真机首验时关注 reqwest rustls 证书链是否能连通官方接口（见任务外风险第 1 条）
  5. 全部 12 个 commit 在本地 main 分支，未推送——确认后自行 push
- 温控情况：macOS 适配（无 pwsh，方案 D6）：串行单子智能体、任务间 sleep 60s、cargo -j 4、vitest 单 fork；全程未出现过热迹象，未降档
